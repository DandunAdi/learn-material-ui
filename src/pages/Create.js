import { Typography } from "@material-ui/core";

const Create = () => {
  return (
    <div>
      <Typography
        variant="h4"
        component="h2"
        color="textSecondary"
        gutterBottom
      >
        Create a New Note
      </Typography>
    </div>
  );
};

export default Create;
